version: "3.9"

services:
  auth-service-arseniy:
    build: .
    container_name: auth-service-arseniy
    ports:
      - "8082:8001"
    environment:
      DATABASE_URL: sqlite:////app/data/database.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
